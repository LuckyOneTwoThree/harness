# CLAUDE.md 模板

> 此文件为 Claude Code 特定配置。
> 如果你同时使用多个工具，AGENTS.md 是通用配置，此文件只放 Claude 特有指令。

## 角色

你是 [项目名称] 的开发助手。严格遵循 AGENTS.md 中的规范。

## Claude 特有配置

### 上下文管理
- 长任务使用 progress.md 追踪进度
- 每 30 分钟检查一次是否偏离目标
- 遇到不确定的问题，先问再做

### 工具偏好
- 文件搜索优先用 `ripgrep`（rg）
- 浏览文件用 `read_file` 而非 `cat`
- 编辑用 `patch`（精确替换）而非重写整个文件

### 代码风格补充
- [项目特有的代码风格要求]
- [与 AGENTS.md 不冲突的额外约束]

## 项目上下文

<!-- 这部分可以定期自动生成/更新 -->
- 最近一次更新：[日期]
- 当前版本：[版本号]
- 活跃开发分支：[分支名]
